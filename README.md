# sinatra-notebook
A small app for sharing lessons with music students.

To run:
```ruby
bundle install
```
```ruby
shotgun
```
located at `http://localhost:9393`

If Cloud9IDE
```ruby
shotgun -p $PORT -o $IP
```